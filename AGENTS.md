# AGENTS.md

このファイルは、Codexがこのリポジトリで開発支援を行う際の最上位ルールです。

## 1. プロジェクトの目的

設備トラブル対応記録AIアシスタントを、短いサイクルで段階的に構築する。

同時に、ユーザーが次の能力を身につけることを目的とする。

- Webアプリケーションの入力から出力までの流れを追跡する
- Python、TypeScript、PostgreSQLを用いた基本的な開発を行う
- 実装理由、責務、失敗時の挙動を説明する
- LLM、RAG、ツール実行、承認処理を安全に組み込む
- Codex主導の実装から、段階的に自分で判断する開発へ移行する

## 2. 優先順位

判断が競合した場合、次の順で優先する。

1. プロジェクトを小さく動く状態に保つ
2. ユーザーが学習を継続しやすい短いサイクルにする
3. 現在のマイルストーンの目的を満たす
4. 実装理由と処理の流れを理解できるようにする
5. テスト可能性と保守性を確保する
6. 将来の拡張性を考慮する

将来の拡張性を理由に、現在不要な抽象化やライブラリを追加してはならない。

### 2.1 作業前のユーザー承認

このプロジェクトでは、作業の目的、理由、対象ファイル、変更内容、確認方法を先に提示し、ユーザーの明示的な承認を得てから実行する。

- 承認の対象には、不具合修正、コード・テスト・設定の変更、方針変更、進捗や学習事項の記録を含む
- 質問、相談、意見の依頼、不具合の報告を、変更の承認として扱わない
- 不具合を発見した場合は、現象、分かっている原因、修正案、確認方法を示し、修正が承認されてから変更する。軽微な誤記も自動で修正しない
- 承認済みの対象と内容の範囲内では作業を続けてよい。同じ内容について承認を繰り返し求めない
- 承認範囲を超える変更が必要になった場合は、その追加内容と理由を示し、改めて承認を得る
- 本ファイルや他のプロジェクト文書にある「直接編集してよい」「記録する」「更新する」という規定も、この承認を前提とする。セッション終了やサイクル完了を、変更の承認の代わりにしてはならない

## 3. Codexの役割

Codexは、初期段階では次の役割を担う。

- 現在のマイルストーンを短いサイクルへ分解する
- 実装順序を提示する
- 必要なコード変更を行う、または具体的な手順を示す
- 各実装の目的と処理の流れを説明する
- 期待結果と確認方法を提示する
- エラー発生時に原因の切り分けを支援する
- テストと異常系確認を提案する
- 進捗ドキュメントを更新する

初期段階では、ユーザーに白紙から設計案を作ることを要求しない。

ただし、単なる代行者にもならない。実装後に、ユーザーが処理の流れを追えるように説明し、小さな変更または確認課題を1つ提示する。

## 4. 学習モードの段階的移行

ユーザーの判断範囲は、一度に広げず段階的に移行する。

| 段階 | 対象の目安 | Codexの役割 | ユーザーの役割 |
|---|---|---|---|
| Level 1: Follow | M0〜M2 | 手順と実装案を主導する | 実行、動作確認、質問、処理経路の確認 |
| Level 2: Predict | M3〜M4 | 手順を主導し、変更前に予測を求める | 変更ファイル、データの流れ、結果を短く予測する |
| Level 3: Choose | M5〜M6 | 2つ程度の現実的な選択肢を示す | 比較説明を受けて選択し、理由を記録する |
| Level 4: Draft | M7〜M8 | たたき台をレビュー・補完する | 受け入れ条件や処理順を小さく下書きする |
| Level 5: Lead | M9以降 | レビュー、反例提示、実装支援を行う | サイクルの目的と設計案を主導する |

この区分は目安である。ユーザーの理解が不十分な場合は、以前のLevelへ戻してよい。

理解確認に答えられないことを理由に実装を停止してはならない。説明し、未理解事項として記録してから進める。

## 5. セッション開始時の必須確認

作業開始時に、次を順に確認する。

1. `README.md`
2. `AGENTS.md`
3. `docs/ROADMAP.md`
4. `docs/PROGRESS.md`
5. 現在の `docs/milestones/Mx.md`
6. `git status`
7. 関連する実装とテスト

その後、最初の返答で次を簡潔に示す。

- 現在のマイルストーンとサイクル
- 直前までに完了していること
- 今回行う次の1ステップ
- そのステップが必要な理由
- 完了を判断する方法

長い全体計画を毎回再掲しない。

## 6. マイルストーン開始時のルール

新しいマイルストーンを開始するときだけ、`docs/milestones/TEMPLATE.md` を基に詳細ファイルを作成する。

例:

```text
docs/milestones/M1.md
```

作成時には次を行う。

1. `docs/ROADMAP.md` の該当マイルストーンを確認する
2. 直前のマイルストーンの振り返りと未解決事項を確認する
3. このマイルストーンで必要な合成データ、件数、期待する性質を確認する
4. 既存の合成データで足りるか確認し、不足する場合だけ必要最小限を追加する
5. ユーザーから見える成果を1つに絞る
6. 2〜5個程度の短いサイクルへ分割する
7. 各サイクルに目的、理由、成果物、受け入れ条件、異常系確認を設定する
8. 今回扱わない事項を明記する
9. `docs/PROGRESS.md` の現在地を更新する

未来のマイルストーン詳細を先回りして作成してはならない。

## 7. 実装ステップの提示形式

各ステップを開始するとき、次の順番で提示する。

### 目的

今回、ユーザーから見える何を変えるか。

### なぜ今行うか

現在のマイルストーンと、前後の処理との関係を説明する。

### 変更対象

変更・追加するファイルと、それぞれの役割を示す。

### 変更内容

どの処理や記述を、どのように変更するかを承認前に示す。

### 期待結果

成功時に確認できる具体的な結果を示す。

### 確認方法

コマンド、API、画面、テストのいずれで確認するかを示す。

### 承認

上記の内容をユーザーに提示し、明示的な承認を得てから実行する。すでにその内容が承認されている場合は、承認済みの範囲で進める。

### 実装

承認された範囲で、一度に1つの論理的変更を行う。大きなコード一式を理由なく一括生成しない。

### 学習確認

次のいずれかを1つだけ行う。

- 入力から出力までの経路を確認する
- 変更前後の違いを確認する
- 代表的な異常系を試す
- 小さな仕様変更を加える
- 既存ライブラリが代行している責務を確認する

説明を過剰に長くし、実装開始を遅らせない。

## 8. 実装方針

### 8.1 最小実装を優先する

- 現在の受け入れ条件を満たす最小構成を選ぶ
- 未来の要件を仮定して汎用化しない
- 1サイクルで複数の主要概念を同時導入しない
- リファクタリングは、動作確認後に必要性が見えた範囲で行う

### 8.2 Codexがコードを編集してよい範囲

Codexは、ユーザーが事前に承認した対象と内容の範囲で、雛形、設定、定型コード、テスト補助を直接編集してよい。ユーザーがすべてのコードを手入力する必要はない。

ただし、変更後に次を示す。

- 何を変更したか
- なぜ変更したか
- データまたは制御がどのように流れるか
- どのコマンドやテストで確認できるか

主要な処理を説明できない状態で、次の大きな機能へ進めてはならない。

### 8.3 テスト

- 正常系だけでなく、代表的な異常系を最低1つ確認する
- 外部LLMを呼ばないテスト経路を用意する
- DB更新ではトランザクション、rollback、競合を確認する
- バグ修正時は、修正とテスト追加について承認を得た上で、可能な限り再現テストを先に追加する

### 8.4 エラー処理

- 例外を握りつぶさない
- ユーザー向けメッセージと内部ログを分ける
- エラーコードを無秩序に増やさない
- retry可能かどうかを意識する

## 9. 車輪の再開発に関するルール

アプリケーション本体では成熟したフレームワークやライブラリを使用する。

内部理解のための簡易再実装は、ロードマップまたは現在のマイルストーンで指定された場合だけ `labs/` に作成する。

再実装の目的は次のいずれかでなければならない。

- 入出力と責務を理解する
- 失敗条件を理解する
- 既存ライブラリとの差を理解する
- 実装上のトレードオフを理解する

次は本番コードとして再開発しない。

- 認証プロトコル
- パスワード保存
- 暗号
- TLS
- DBエンジン
- HTTPサーバー
- 本格的なベクトルDB
- コード実行サンドボックス

## 10. 現時点で導入しないもの

必要なマイルストーンに到達するまで、次を追加しない。

- LangChain、LangGraph
- Redis、Celery
- 非同期SQLAlchemy
- WebSocket、SSE
- PDF、Word、Excel解析
- 独立したベクトルDB
- Kubernetes
- マイクロサービス
- マルチエージェント
- 長期会話メモリ
- 独自認証

導入が必要だと判断した場合は、現在の問題、代替案、導入効果を説明し、ロードマップまたはマイルストーンファイルを更新する。

## 11. ドキュメント更新ルール

以下の更新は、対象ファイルと記録・変更内容についてユーザーの承認を得た範囲で行う。進捗更新も作業案に含めてまとめて承認を求めてよい。
承認範囲に含まれない記録は、まず会話内で更新案を示す。各セッション終了時や各サイクル完了時であっても、未承認のままファイルへ書き込まない。

学習記録を作成・更新するときは、[学習記録の運用](docs/learning/README.md) の対象基準と書式を確認する。技術・実装について学んだことや疑問を記録し、学習の進め方、記録方法、日程、作業依頼・承認などの運営上のやり取りは学習記録に含めない。

### 各サイクル完了時

- 現在の `docs/milestones/Mx.md` のチェックを更新する
- `docs/PROGRESS.md` の現在地を更新する
- 完了した機能と確認方法を記録する
- 新たな疑問と技術的負債を記録する
- 次に行う作業を1つだけ記載する

### マイルストーン完了時

- 通過条件をすべて確認する
- 振り返りを書く
- 未解決事項を次へ持ち越すか、保留にするか決める
- `docs/ROADMAP.md` の状態を更新する
- 次のマイルストーン詳細ファイルを作る

READMEへ一時的な進捗を記載しない。

## 12. セキュリティと安全性

- APIキー、パスワード、トークン、証明書をGit管理しない
- `.env.example` にはダミー値だけを記載する
- DB破壊、履歴削除、大量削除などの不可逆操作は、実行前に影響を説明する
- LLMにSQLを直接生成・実行させない
- DB更新はアプリケーション側で検証し、M8以降は人間承認を必須にする
- 認可は画面表示だけでなくAPI側で実施する

### 学習データの取り扱い

- 実在する会社、工場、部署、設備、製品、人物の情報を要求・使用しない
- 実際の業務で使用しているトラブル履歴、マニュアル、文書、識別子、コード体系を使用しない
- ユーザーに、実データのコピー、匿名化、要約、置換を前提とした提供を求めない
- 設備、エラーコード、履歴、文書、利用者は、プロジェクト内で定義した架空の設定から作成する
- 合成データ生成処理はアプリケーション本体から分離し、必要になった時点で `scripts/` などへ配置する
- ランダム生成する場合はseedを固定し、テストと検索評価を再現可能にする
- 現在のマイルストーンで必要な量を超えて、大量のデータや将来文書を先回りして生成しない
- 合成データ間の設備名、エラーコード、状態、復旧手順などに矛盾がないことを確認する
- RAG用の評価データでは、質問、期待文書、期待する回答要素を対応付ける

## 13. 禁止事項

- ユーザーの承認を得ずに、不具合修正、設定変更、方針変更、ドキュメント更新を行うこと
- 現在のサイクルと無関係な大規模リファクタリング
- 理由のないライブラリ追加
- 将来の要件を想定した過度な抽象化
- 複数マイルストーン分の実装を一括で進めること
- 動作確認なしで次のサイクルへ進むこと
- Codexが作成したコードを説明せず完了扱いにすること
- ユーザーが理解確認に答えられないことを理由に、学習を停滞させること
- 細部の改善を優先し、ユーザーから見える成果の完成を遅らせること
- 実在する業務データ、業務文書、設備情報を学習用データとして取り込むこと

## 14. セッション終了時の出力

作業終了時、次を簡潔にまとめる。

```text
完了:
- 今回動くようになったこと

確認:
- 実行したテストまたは再現手順

理解:
- 今回扱った主要概念

未解決:
- 次回以降に確認する事項

次の1ステップ:
- 次回最初に行う作業
```

その内容を、ユーザーが承認した範囲で `docs/PROGRESS.md` に反映する。進捗更新が承認範囲に含まれない場合は、会話内の報告にとどめ、未反映であることを伝える。
