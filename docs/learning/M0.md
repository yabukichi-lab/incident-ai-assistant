# M0の学習記録

対象: [M0: 開発基盤とエンドツーエンド疎通](../milestones/M0.md)

[記録ルールと書式](README.md)に沿って、質問と各ステップの学びをまとめます。
質問・回答・本人の説明は、学び直しに必要な要点を要約しています。2026-09-04分は既存の進捗文書を基に整理したため、質問の内容を確認できない箇所があります。2026-09-09分は同日の会話を基に整理しました。

## 目次

| ID | 日付 | サイクル／ステップ | テーマ | 理解の確認状況 |
|---|---|---|---|---|
| [L-M0-001](#l-m0-001) | 2026-09-04 | M0-C1／環境準備 | 仮想環境とPythonの指定 | 一部理解の記録あり。本人の説明内容は未確認 |
| [L-M0-002](#l-m0-002) | 2026-09-04 | M0-C1／health APIの実装・正常応答確認 | URLとPython関数の対応 | 説明済み。本人による処理経路の説明は未確認 |
| [L-M0-004](#l-m0-004) | 2026-09-09 | M0-C1／処理経路の確認 | UvicornとFastAPIの関係 | 9/14に本人が経路を説明。URL対応・Uvicornの受付は説明できた。JSON変換の担当は未理解 |
| [L-M0-005](#l-m0-005) | 2026-09-14 | M0-C1／テスト追加・停止再起動確認・application追加 | TestClientによるHTTPテストと実行ディレクトリ | テストの各行と失敗予測を本人が説明。TestClientがサーバーを起動しない点は補足 |

L-M0-003は運営上の話題であったため記録対象外として削除済み。IDは再利用しないため欠番とする。

## L-M0-001

- 日付: 2026-09-04（9/7予定分を前倒し）
- 記録日: 2026-09-09
- サイクル／ステップ: M0-C1／バックエンド環境の準備
- テーマ: 仮想環境とPythonの指定
- きっかけ: 環境構築、説明・確認

### 学ぶこと・確かめたいこと

プロジェクト専用のPython環境と依存関係を用意し、どのPythonで実行するかを確認する。

### 当初の理解・疑問

当時の説明記録には、仮想環境、`--python`、Gitの除外確認が登場する。本人の質問や当初の考えは、既存記録から確認できない。

### 取り組んだこと

当時の進捗記録によると、ユーザーがuvでバックエンドの最小構成と仮想環境を作成した。
Codexが依存関係ファイルとlockfileの存在、専用PythonからのFastAPI・Uvicornのimport成功を確認し、Git除外設定の誤記を修正した。今回の作業では環境の再検証はしていない。

### 質問と回答

- 質問: 内容未確認。既存の進捗文書には回答の要点があるが、具体的な質問内容は確認できない。
- 回答（既存記録の要約）: `.venv` には設定だけでなく、Python実行用のリンクやライブラリも含まれる。`--python` は使用するPythonの指定で、`git check-ignore` はGitの除外状態を調べるためのコマンド。
- 具体例（当時の確認記録）: `.venv/bin/python` は `/usr/bin/python3` を参照し、Python 3.12.3を使っていた。

### 得られた知識・気づき

扱った内容は、プロジェクト専用の環境から実行すること、使用するPythonの指定、Git管理からの除外確認。

### 理解の確認

- 確認状況: 一部理解の記録あり。本人の説明内容は未確認。
- 根拠（既存記録の要約）: 仮想環境のPythonを使う点は理解したと進捗文書に記録されている。具体的な本人の説明や、それ以外の理解確認は記録から判断できず、今回も再確認していない。

### 残る疑問

仮想環境・Python指定・Git除外の違いを自分の言葉で説明できるかは未確認。

### 参照

- [PROGRESS.md](../PROGRESS.md) 第5節、2026-09-04「ユーザーが作成したバックエンド環境を確認し、9/7予定分を完了」
- [依存関係の定義](../../backend/pyproject.toml)

### 後日の振り返り

未実施。

## L-M0-002

- 日付: 2026-09-04（9/8予定分を前倒し）
- 記録日: 2026-09-09
- サイクル／ステップ: M0-C1／health APIの実装・正常応答確認
- テーマ: URLとPython関数の対応
- きっかけ: 実装、不具合の報告、コード確認

### 学ぶこと・確かめたいこと

`GET /health` がどの関数へ対応し、戻り値がJSONレスポンスになるかを確認する。

### 当初の理解・疑問

当時の記録では、Uvicornは起動していたが `/health` へのアクセスが404になっていた。本人の具体的な質問や原因の予想は未確認。

### 取り組んだこと

当時の進捗記録によると、デコレーターのURLが `/heath` になっていたため、Codexが `/health` に修正し、ルート一覧を比較した。
その後、ユーザーから問題解消の報告があり、正常応答確認を完了とした。Codexによる修正後のHTTP接続確認は成功しておらず、正常応答の根拠はユーザー報告。

### 質問と回答

- 質問: 内容未確認。
- 報告（既存記録の要約）: Uvicornは起動したが、`/health` へのアクセスが404になった。
- 回答（既存記録の要約）: URLを決めるのはPythonの関数名ではなくデコレーター。`@app.get("/health")` が `GET /health` と関数を対応付ける。
- 具体例: [main.py](../../backend/main.py) では、そのデコレーターを付けた `health()` が `{"backend": "ok"}` を返す。

### 得られた知識・気づき

扱った内容は、サーバーが起動していてもURLが一致しなければ404になることと、デコレーターでURLと処理を対応付けること。

### 理解の確認

- 確認状況: 説明済み。本人による処理経路の説明は未確認。
- 根拠: 問題解消の報告と、処理経路を説明できることは別に扱う。当時の記録でも、Uvicorn・FastAPI・`health()`・JSONの関係を本人が説明する確認は次の作業になっている。

### 残る疑問

リクエストからJSONレスポンスまでの処理経路を、コードと対応付けて説明できるかを確認する。

### 参照

- [PROGRESS.md](../PROGRESS.md) 第5節、2026-09-04のURL誤記修正と問題解消報告
- [M0-C1の学習目的と確認課題](../milestones/M0.md)
- [現在のhealth API](../../backend/main.py)

### 後日の振り返り

未実施。

## L-M0-004

- 日付: 2026-09-09
- 記録日: 2026-09-09
- サイクル／ステップ: M0-C1／リクエストからJSON応答までの処理経路の確認
- テーマ: UvicornとFastAPIの関係
- きっかけ: 処理経路の説明、関連質問、Uvicorn停止時の挙動についての本人の報告

### 学ぶこと・確かめたいこと

Uvicorn、FastAPI、自分で書く関数の役割を区別する。HTTP通信とPython内部の呼び出しの境界、接続失敗と404の違い、アプリ開発で実装する範囲を理解する。

### 当初の理解・疑問

本人は、UvicornとFastAPIが別々のサーバーとしてHTTP通信しているのか、なぜ両者が分かれているのかを質問した。また、開発時にUvicorn側にも処理を実装するのか、起動時の指定だけを扱うのかを確認した。

### 取り組んだこと

既存の `GET /health`、`health()`、起動コマンド `uv run uvicorn main:app --reload` を例に、通信とアプリケーション内部の処理を対応付けて説明した。
本人は、Uvicorn停止時にブラウザで「このサイトにアクセスできません」と表示されることを報告し、404との違いを自分の言葉で説明した。
Codexは公式ドキュメントを参照して回答した。今回の記録作業ではコード変更、サーバー起動、HTTP通信の再検証は行っていない。

### 質問と回答

質問と回答を論点ごとに要約する。

**質問1（要約）: Uvicornの役割**

Uvicornは何を担当し、FastAPIとどのような関係にあるのか。

回答（要約）:

UvicornはHTTP通信を受け付け、FastAPIで作ったアプリケーションを動かすサーバープログラム。FastAPIはURLに対応する処理の選択、入力検証、レスポンス作成を担当し、本人が書く `health()` などの関数がアプリ固有の処理を担当する。

具体例:

```text
ブラウザ／curl
  → HTTP通信 → Uvicorn
  → Python内部でFastAPIのappを呼ぶ
  → FastAPIがhealth()を呼ぶ
  → 辞書を返す
  → FastAPIがJSONの応答を作る
  → UvicornがHTTPレスポンスを送る
  → ブラウザ／curlが表示する
```

**質問2（要約）: 接続失敗と404の違い**

Uvicorn停止時のアクセス失敗は、404とは違い、URLの有無を確認する前に接続で失敗しているという理解でよいか。

回答（要約）:

その理解で合っている。停止時は接続する段階で失敗し、HTTPレスポンスを受け取れていない。「このサイトにアクセスできません」はブラウザ自身のメッセージ。一方、404はサーバーに接続でき、FastAPIがURLを調べた結果として返すHTTPレスポンス。接続できなければ `/health` の存在を調べる段階まで進めない。

**質問3（要約）: 同じサーバーで動くのか、分離する意味は何か**

UvicornとFastAPIは同じサーバー上で動くのか。両者の間にもHTTP通信があるのか、役割を分ける理由は何か。

回答（要約）:

今の構成では、UvicornとFastAPIで作ったアプリは同じPC上の同じサーバープロセス内で動く。ブラウザとUvicornの間はHTTP通信だが、UvicornからFastAPIへはPython内部で呼び出し、別のHTTP通信は行わない。両者の呼び出し方とデータの受け渡しを定める取り決めがASGI。

「サーバー」という言葉がコンピューターとサーバープログラムの両方を指すことを整理した。通信を扱う仕事をUvicorn、URLの振り分けなどをFastAPIに分けることで、それぞれの仕組みを再利用できる。ASGIに対応する別のサーバープログラムでアプリを動かすこともできる。

補足として、`--reload` のログに出る `reloader process` はファイル監視用の別プロセスであり、Uvicorn用とFastAPI用に分かれたものではないと説明した。最初の説明では「同じプログラム内部で動く」ことを十分に伝えていなかったため、この質問への回答で補足した。

**質問4（要約）: 開発時にUvicornをどこまで扱うか**

FastAPIで開発するとき、Uvicornにも処理を実装するのか。読み込むアプリの指定以外に何を設定するのか。

回答（要約）:

通常はFastAPIを使ったアプリケーションコードを実装し、Uvicornは起動コマンドや設定を通して利用する。Uvicorn本体のコードを編集することは通常ない。読み込むアプリ以外にも、待ち受けアドレス、ポート、自動再読み込みなどを設定する。

```text
uv run   → プロジェクトの環境で、後ろのコマンドを実行する
uvicorn  → サーバープログラムを起動する
main:app → main.py内のappをUvicornに読み込ませる
--reload → Uvicornの自動再読み込みを有効にする
```

具体例として `uv run uvicorn main:app --reload --port 8001` を提示した。同じアプリのまま接続先ポートを変える設定例であり、変更を実行した記録はない。

### 得られた知識・気づき

- UvicornはHTTP通信、FastAPIはURLの振り分けなど、自作関数はアプリ固有の処理を担当する。
- 現在の構成ではUvicornとFastAPIの間に別のHTTP通信はなく、サーバープロセス内でASGIに従って呼び出す。
- 接続失敗はHTTP応答を受け取る前に起こり、404は接続後に返される応答である。
- uvは環境・依存関係の管理とコマンド実行を担当する。Uvicornとは別のツールであり、`main:app` を解釈してアプリを読み込むのはUvicorn。
- アプリ固有の機能は自分のコードに実装し、Uvicornは通信を受け付けるための設定を扱う。

### 理解の確認

- 確認状況: 接続失敗と404の違いは本人が説明。UvicornとFastAPIの役割分担は説明後に理解の表明あり。
- 本人の説明・確認結果: 質問2で、接続できなければURLの存在を確認する前段階で止まると説明した。停止時のブラウザ表示は本人の報告に基づく。
- 本人の理解の表明（要約）: 質問4への回答後、説明を理解したと表明した。
- 確認の限界: 上記の表明は記録するが、ASGI、同じプロセス内の呼び出し、全体の処理経路を本人が自分の言葉で説明できたとまでは判定しない。

### 残る疑問

このやり取りの区切りでは、本人から追加の疑問は提示されていない。後日の振り返りでは、UvicornとFastAPIの役割、HTTP通信と内部呼び出しの境界、JSON応答までの経路を回答を見ずに説明できるか確認する。

### 参照

- 2026-09-09の会話: UvicornとFastAPIの役割、接続失敗と404の違い、起動設定についての質問・回答と本人の確認。
- [既存のURLと関数の学習記録](#l-m0-002)
- [health APIのコード](../../backend/main.py)
- [FastAPI公式: サーバーの起動とASGI](https://fastapi.tiangolo.com/deployment/manually/#asgi-servers)
- [ASGI公式仕様: サーバーとアプリケーションの関係](https://asgi.readthedocs.io/en/latest/specs/main.html#overview)
- [Uvicorn公式: 起動設定](https://uvicorn.dev/settings/)

### 後日の振り返り

**2026-09-14: 回答を見ずに `GET /health` からJSON応答までの経路を説明**

見直したこと: `backend/main.py` を見ながら、URLと関数の対応、Uvicorn・FastAPI・`health()` の役割分担、辞書がJSON応答になる流れ、接続失敗と404の違いを説明した。

説明できたこと（要約）:

- UvicornがHTTPリクエストを受け取ってFastAPIへ渡し、FastAPI側で処理が実行される。
- `@app.get("/health")` が「`/health` へのアクセス時に直下の関数を呼ぶ」という登録なので、FastAPIは `health()` を呼べる。
- 存在しないURL（`/healthz`）へのアクセスは404が返る。

当時の誤解と訂正:

- 辞書 `{"backend": "ok"}` をJSON文字列へ変換するのはUvicornだと答えた。実際はFastAPIが変換し、`Content-Type: application/json` を付けたレスポンスを組み立てる。UvicornはFastAPIが作ったレスポンスをHTTPの形式でバイト列として送るだけで、中身がJSONかどうかは関知しない。
- 覚え方: Uvicornは配達員、FastAPIは荷造り係、`health()` は中身を作る人。配達員は中身を作り替えない。
- 404の説明は合っていたが、対になる「サーバー停止時は接続拒否になり、Uvicornに届く前に失敗する」を自分からは挙げなかった。404は「サーバーは生きていてURLが違う」、接続拒否は「サーバーが動いていない」と切り分ける。

残る疑問: JSON変換の担当（FastAPI）を、次の振り返りで自分の言葉で説明できるか。M0-C1の小さな変更課題（`application` フィールド追加）で、辞書のキーを増やすとJSONのキーも増えることを実際に確認する。

## L-M0-005

- 日付: 2026-09-14
- サイクル／ステップ: M0-C1／health APIのテスト追加、停止・再起動の再現確認、`application` フィールド追加
- テーマ: TestClientによるHTTPテストと実行ディレクトリ
- きっかけ: テストの実装と本人による実行、エラーの切り分け、変更前の予測

### 学ぶこと・確かめたいこと

Uvicornを起動せずにhealth APIを自動テストする仕組みを理解する。停止・再起動で結果が再現することと、停止時の失敗を確認する。辞書にキーを足すとJSON応答が変わることを、テストの失敗予測と実際の応答で確かめる。

### 当初の理解・疑問

L-M0-004の振り返りで、辞書からJSONへの変換をUvicornが担当すると誤解していた。テストについては、サーバーを立ち上げず「テスト用に立ち上げる」ものという理解だった。

### 取り組んだこと

- Claude Codeが `backend/tests/test_health.py` と、開発用依存のpytest・httpx2、pytest設定（`testpaths`、`pythonpath`）を追加した。
- 本人が手元で `uv run pytest` を実行し、1 passedを確認した。
- 本人が手で、起動→curl→停止→curl→再起動→curlを実施し、200、接続拒否、200を確認した。
- Claude Codeが `main.py` の辞書に `"application": "incident-ai-assistant"` を追加し、テストの期待値を更新した。

### 質問と回答

**確認1: テストコードの各行の役割**

本人の説明（要約）: `from main import app` は `main.py` の `app = FastAPI()` を持ってくる。`TestClient(app)` はサーバーを実行するのではなくテスト用の立ち上げ。`client.get("/health")` の戻り値には `/health` にアクセスした結果が入る。2つの `assert` はアクセスに問題がなかったことと、本文が `"backend": "ok"` であることを確かめる。

補足: TestClientはサーバーを立ち上げず、ネットワークのポートも開かない。Python内部で `app` を直接呼び出すため、Uvicornの役割を省略している。`response` にはステータスコード、ヘッダー、本文が入る。

**確認2: `main.py` だけ変えてテストを直さなかった場合の予測**

本人の予測（要約）: テストは失敗する。2つ目の `assert` で、実際の本文が `"backend": "ok"` だけでなく `"application"` を含むため。

結果: 予測は正しい。1つ目の `assert`（200）は通り、2つ目で止まる。pytestは期待した辞書と実際の辞書の差分を表示する。辞書に足したキーがそのままJSONのキーになるので、変換の担当がFastAPI側であることの確認にもなる。

**エラーの切り分け: `Could not import module "main"` と `No module named 'main'`**

本人は、リポジトリ直下で `uv run uvicorn main:app --reload` と `uv run pytest` を実行してこれらのエラーに遭い、`backend/` へ移動していなかったことが原因だと自分で判断した。

補足: Uvicornは `main:app` を「今いるディレクトリの `main.py`」として探す。pytestは直下に `pyproject.toml` がないため `configfile` を見つけられず、`pythonpath = ["."]` が効かない。プロンプト先頭の `(backend)` は仮想環境の名前で、作業ディレクトリとは無関係。ログでは `rootdir` と `configfile` の行で判別できる。

### 得られた知識・気づき

- TestClientはUvicornなしでFastAPIアプリを直接呼び、HTTPと同じ形の結果（ステータス、ヘッダー、本文）を返す。
- `uv run` は仮想環境を自動で見つけるが、Uvicornとpytestがファイルや設定を探す基準は現在のディレクトリ。バックエンドの操作は `backend/` で行う。
- 失敗の3パターン: 接続拒否（サーバーが動いていない）、404（サーバーは生きていてURLが違う）、アプリ読み込み失敗（サーバーは起動しようとしたが `main` を見つけられない）。
- 現行のStarletteはTestClient用に `httpx` ではなく `httpx2` を推奨する（`httpx` でも動くが非推奨警告が出る）。

### 理解の確認

- 確認状況: テストの各行の役割と失敗予測を本人が説明した。TestClientがサーバーを起動しない点は説明後に補足した。実行ディレクトリの誤りは本人が自分で切り分けた。
- 本人の説明・操作・確認結果: 上記の確認1・2と、curl3回の結果報告（2026-09-14の会話）。

### 残る疑問

- `application` の値を変えたとき、変更が必要なファイルの数（現在と、M0-C3で画面表示を追加した後）は未回答。
- `application` を追加した応答を本人の手元でcurlで確認する作業が残っている。

### 参照

- [health APIのコード](../../backend/main.py)
- [health APIのテスト](../../backend/tests/test_health.py)
- [L-M0-004: UvicornとFastAPIの関係](#l-m0-004)
- [FastAPI公式: テスト](https://fastapi.tiangolo.com/tutorial/testing/)

### 後日の振り返り

未実施。
