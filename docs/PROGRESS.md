# 進捗管理

このファイルは、プロジェクトの現在地と次に行う作業の正本です。

Codexは原則として各セッション終了時に更新します。次のセッションでは、このファイルから作業を再開します。

## 1. 現在地

| 項目 | 現在値 |
|---|---|
| 現在のマイルストーン | M0: 開発基盤とエンドツーエンド疎通 |
| 現在のサイクル | M0-C1: バックエンドの最小health API |
| 状態 | In progress（9/7予定の環境準備完了、health API実装待ち） |
| 学習モード | Level 1: Follow |
| 第一完成地点 | M8 |
| 詳細計画 | `docs/milestones/M0.md` |
| 全体計画 | `docs/SCHEDULE.md`（2026-09-07〜2027-08-22、50週間） |

## 2. 次に行う1ステップ

```text
FastAPIアプリケーションと GET /health を作り、Uvicornで起動して
HTTP 200 と backend: "ok" を含むJSONを返すことを確認する。
```

### 完了確認

- FastAPIアプリケーションをUvicornで起動できる
- `GET /health` がHTTP 200を返す
- JSONレスポンスに `backend: "ok"` が含まれる

9/7予定の環境準備は完了した。次は9/8予定のAPI実装を進める。
M0-C1全体の完了条件は `docs/milestones/M0.md` で管理する。

### 直近の週目標（2026-09-04確認）

既存の週次計画に合わせ、直近の開発週はW01（2026-09-07〜09-13）とする。
9/4〜9/6は目的と手順を確認する準備期間とし、W01の目標はM0-C1の完了に絞る。
日別の時間配分は標準12時間を基にした目安であり、実績に合わせて調整する。

| 日付 | 目安 | 具体的なアクション | 確認する結果 |
|---|---:|---|---|
| 9/7（月） | 1時間 | Pythonなどの利用環境を確認し、`backend/` の最小構成と依存関係を用意する | Python環境と必要なパッケージを利用できる |
| 9/8（火） | 1.5時間 | FastAPIアプリケーションと `GET /health` を作り、起動する | HTTP 200と `backend: "ok"` を返す |
| 9/9（水） | 1時間 | curlで応答を確認し、リクエスト、ルート、Python関数、JSON応答の順にコードを追う | 処理の経路を短く説明できる |
| 9/10（木） | 1.5時間 | ステータスコードとレスポンス内容を確認するpytestを追加する | health APIの自動テストが通る |
| 9/11（金） | 1時間 | 起動・確認・テストの実行手順と疑問点を整理する | 同じコマンドで確認を繰り返せる |
| 9/12（土） | 3時間 | バックエンド停止時の接続失敗と、再起動後の復旧を確認する。不具合があれば修正する | 停止時は接続できず、再起動後は再び200になる |
| 9/13（日） | 3時間 | `application` フィールドを追加してテストを更新し、M0-C1の受け入れ条件と進捗を確認する | 小さな変更後もテストが通り、結果と未理解事項が記録される |

週の完了条件は、正常応答、自動テスト、停止時の失敗、再起動後の再現、処理経路の説明、小さな変更と記録がそろうこと。
PostgreSQLへの接続はW02、Reactからの表示とM0全体の確認はW03に予定している。

## 3. マイルストーン進捗

- [ ] M0: 開発基盤とエンドツーエンド疎通
- [ ] M1: トラブル記録の読み取り
- [ ] M2: 登録・更新と安全性
- [ ] M3: 検索と保守可能な構造
- [ ] M4: LLMによる単機能
- [ ] M5: 最小RAG
- [ ] M6: ベクトル検索と評価
- [ ] M7: 読み取り専用エージェント
- [ ] M8: 人間承認付きDB更新
- [ ] M9: 実務品質

## 4. 現在のマイルストーン進捗

### M0-C1: バックエンドの最小health API

- [x] Pythonと環境管理ツールを確認する（Python 3.12.3、uv 0.11.33）
- [x] `backend/` の最小構成を作成する
- [ ] FastAPIアプリケーションを起動する
- [ ] `GET /health` を実装する
- [ ] `curl` またはブラウザでHTTP 200を確認する
- [ ] health APIのテストを追加する
- [ ] リクエストからレスポンスまでの流れを確認する

### M0-C2: PostgreSQLとバックエンドの疎通

- [ ] Docker ComposeでPostgreSQLを起動する
- [ ] 環境変数からDB接続情報を読む
- [ ] バックエンドから `SELECT 1` を実行する
- [ ] health APIへDB状態を追加する
- [ ] DB停止時の応答を確認する

### M0-C3: Reactとバックエンドの疎通

- [ ] `frontend/` を作成する
- [ ] React画面を起動する
- [ ] health APIを呼び出す
- [ ] Frontend、Backend、Databaseの状態を表示する
- [ ] loading、success、errorを確認する

### M0-C4: エンドツーエンド確認と再現手順

- [ ] 新しい状態から起動手順を再現する
- [ ] バックエンド停止時の画面表示を確認する
- [ ] DB停止時の画面表示を確認する
- [ ] READMEへ起動・確認手順を反映する
- [ ] M0の通過条件を確認する
- [ ] M0の振り返りを記録する

## 5. 完了したこと

- 2026-08-13: 初期ドキュメント構成を作成
  - `README.md`
  - `AGENTS.md`
  - `docs/ROADMAP.md`
  - `docs/PROGRESS.md`
  - `docs/milestones/README.md`
  - `docs/milestones/TEMPLATE.md`
  - `docs/milestones/M0.md`
- 2026-08-14: 合成データ方針をドキュメントへ反映
  - テーマは設備トラブル対応のまま継続
  - 実在する業務データは使用しない
  - 各マイルストーンで必要な合成データだけを段階的に追加
- 2026-09-03: 学習時間を基に全体期間と週次目標を設定
  - M8第一完成を2027-06-13、M9機能完了を2027-08-08に設定
  - 予備期間と最終確認を含む50週間の計画を `docs/SCHEDULE.md` に作成
  - 未来の詳細サイクルは各マイルストーン開始時に作る方針を維持
- 2026-09-04: プロジェクト概要と直近の週目標を確認
  - 文書とファイル構成から、M0-C1着手前で実装コード・テストがないことを確認
  - W01の目標を最小health APIの完成とし、標準12時間の日別アクションを整理
  - 今回は計画確認のみ。実装・テスト実行・環境の動作確認は未実施
- 2026-09-04: 9/7予定分を前倒しで開始し、環境を確認
  - Python 3.12.3、uv 0.11.33、Node.js v24.15.0、Docker Compose CLI v5.3.1を確認
  - システムPythonにpipとensurepipがないことを確認し、既存のuvで環境と依存関係を管理する手順を提示
  - 仮想環境・依存関係・Git除外設定の作成はユーザーが手順を実行後に確認する。API実装・テスト・Dockerの起動確認は未実施
- 2026-09-04: ユーザーが作成したバックエンド環境を確認し、9/7予定分を完了
  - `pyproject.toml`、`uv.lock`、`.venv` の存在と、専用PythonからのFastAPI・Uvicornのimport成功を確認
  - `.venv/bin/python` が `/usr/bin/python3` を参照し、Python 3.12.3を使っていることを確認
  - ルート `.gitignore` の `.env/` を予定どおり `.venv/` に修正。修正前はuvが仮想環境内に作った `.gitignore` によって除外されていた
  - ユーザーは仮想環境のPythonを使う点を理解。`.venv` には設定に加えPython実行用のリンクやライブラリもあること、`--python` は使用するPythonの指定であること、`git check-ignore` は除外状態を調べるコマンドであることを補足
  - API実装とAPIテストは未着手

## 6. 現在のブロッカー

なし。

ブロッカーが発生した場合は、次の形式で記録します。

```text
- 発生日:
- 現象:
- 再現手順:
- 期待結果:
- 実際の結果:
- 試したこと:
- 次に確認すること:
```

## 7. 未理解・後で深掘りする事項

現時点ではなし。

実装を止めずに後で確認する疑問を、次の形式で追加します。

```text
- 疑問:
- どの処理で出てきたか:
- 今すぐ理解が必要か: Yes / No
- 確認する予定のマイルストーン:
```

## 8. 保留している改善案

現時点ではなし。

現在のマイルストーンに不要な案は、実装せずここへ記録します。

```text
- 改善案:
- 期待効果:
- 今は実装しない理由:
- 再検討する時期:
```

## 9. 重要な決定事項

| 日付 | 決定 | 理由 | 影響 |
|---|---|---|---|
| 2026-08-14 | 設備トラブル対応というテーマを継続する | Webアプリ、RAG、エージェント、DB更新を一貫して学べるため | ロードマップのテーマとマイルストーンは変更しない |
| 2026-08-14 | 実際の業務データを使用しない | 業務外の個人学習であり、会社情報や業務情報を持ち出さないため | 設備、履歴、文書、利用者はすべて架空の合成データとする |
| 2026-08-14 | 合成データを必要な時点で段階的に作る | 過剰な事前設計を避け、短い開発サイクルを維持するため | M1以降の開始時に、必要件数と内容だけを具体化する |
| 2026-09-04 | バックエンドのPython環境と依存関係を既存のuvで管理する | uvが利用でき、システムPythonにはpipとensurepipがないため | `backend/pyproject.toml` と `backend/uv.lock` で依存関係を記録し、専用の `.venv/` を使う |

## 10. セッションログ

| 日付 | 実施内容 | 確認結果 | 次の1ステップ |
|---|---|---|---|
| 2026-08-13 | 初期ドキュメントを作成 | M0を開始できる構成を準備 | FastAPIの最小health APIを作る |
| 2026-08-14 | 合成データの利用方針を決定し、関連ドキュメントを更新 | テーマを維持し、実データを使わず段階的に合成データを追加する方針を明文化 | FastAPIの最小health APIを作る |
| 2026-09-03 | 週9〜16時間を前提にプロジェクト期間、マイルストーン期間、週次目標を設定 | M0〜M9と予備期間を50週間へ割り当てた | FastAPIの最小health APIを作る |
| 2026-09-04 | 概要、現在地、W01の目標と日別アクションを整理 | 文書とファイル構成を照合。M0-C1は未着手。HTTPリクエストからJSON応答までを最初の学習対象とし、実装・テスト・環境確認は次回実施 | FastAPIの最小health APIを作る |
| 2026-09-04 | 9/7予定分の環境確認を実施し、構成作成のアクションを提示 | Pythonとuvが利用可能。pipとensurepipの不足を確認。仮想環境と依存関係の役割を説明する手順を整理。構成作成とimport確認は未実施 | uvでバックエンドの最小構成と依存関係を用意する |
| 2026-09-04 | ユーザーの環境構築を検証し、仮想環境・Python指定・Git除外確認を説明 | 専用Pythonでimport成功。Git除外設定の誤記を修正。9/7予定分は完了、APIは未実装 | FastAPIの `GET /health` を作り、起動と正常応答を確認する |

## 11. セッション終了時の更新項目

毎回、最低限次を更新します。

- 現在のマイルストーン
- 現在のサイクル
- 状態
- 次に行う1ステップ
- 完了したこと
- ブロッカー
- 未理解事項
- 重要な決定事項（新しい判断がある場合）
- セッションログ

## 12. 再開用プロンプト

```text
README.md、AGENTS.md、docs/ROADMAP.md、docs/PROGRESS.md、
現在の docs/milestones/Mx.md を読んでください。

現在地を確認し、docs/PROGRESS.md の「次に行う1ステップ」から再開してください。
今回は1つの論理的変更だけ進め、目的、変更対象、期待結果、確認方法を先に説明してください。
完了後は進捗ファイルを更新してください。
```
