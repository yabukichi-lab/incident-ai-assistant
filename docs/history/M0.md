# M0の作業履歴

現在地と次の作業は [PROGRESS.md](../PROGRESS.md)、受け入れ条件は [M0.md](../milestones/M0.md) を参照する。このファイルは必要な履歴を調べるときだけ読み、過去の提案や状態を現在の指示として扱わない。

## 2026-09-09：開始時の読み込みを縮小

- 目的：作業再開時に必要な情報へ参照範囲を絞り、不要な文書の全文読み込みを減らす。
- 計画：AGENTS.mdの重複整理と読み込み条件の明確化、PROGRESS.mdの短縮と旧記録の移管、README.mdの開始手順更新。
- 承認：ユーザーの「計画作成後、修正を実行してください」という依頼に基づく。
- 変更：通常の再開は進捗、Git状態、現在のサイクル、関連コードを確認する。概要・全体計画・履歴・学習記録は必要なときに該当箇所を読む。既存の承認・安全・学習規則を維持する。
- 確認：`git diff --check`、移管した記録の一致（相対リンクのみ調整）、承認・学習段階・安全規則の一致、現在地と対象外ファイルの維持、16個のローカルリンクと見出しを検証した。進捗が欠落・不明・矛盾する場合の追加確認と、完了状態を推測しない手順も確認した。文書のみの変更のためアプリのテストは実行していない。
- 文字数：従来の開始対象5文書は計33,975文字。新しい通常の参照範囲（AGENTS.md、PROGRESS.md、M0の基本情報・スコープ・M0-C1）は約8,900文字で、約74%縮小。進捗文書は約8,100文字から約1,600文字へ縮小した。コード・ツール結果・アプリ共通の指示はこの比較に含めていない。
- 理解確認として提示すること：通常の再開と、過去の判断を調べる場面で、読む文書の範囲がどう変わるかを確認する。本人による回答は未確認。
- 未解決：新しい会話での実トークン表示は未測定。文字数の削減率は、会話全体のトークン削減率とは異なる。
- 次の開発作業：M0-C1のGET /healthからJSON応答までの処理経路確認。

## 移管時点の過去の記録（2026-09-09保存）

以下は従来のPROGRESS.mdの週次計画および第3〜10節を移したもの（相対リンクのみ移動先に合わせて調整）。日付、チェック状態、当時の未採用案も当時の記録として保持する。新たな進捗はここへ重ねて書き換えず、上の履歴に日付付きで記録する。

### 直近の週目標（2026-09-04確認）

既存の週次計画に合わせ、直近の開発週はW01（2026-09-07〜09-13）とする。
9/4〜9/6は目的と手順を確認する準備期間とし、W01の目標はM0-C1の完了に絞る。
日別の時間配分は標準12時間を基にした目安であり、実績に合わせて調整する。

| 日付 | 目安 | 具体的なアクション | 確認する結果 |
|---|---:|---|---|
| 9/7（月） | 1時間 | Pythonなどの利用環境を確認し、`backend/` の最小構成と依存関係を用意する | Python環境と必要なパッケージを利用できる |
| 9/8（火） | 1.5時間 | FastAPIアプリケーションと `GET /health` を作り、起動する | HTTP 200と `backend: "ok"` を返す |
| 9/9（水） | 1時間 | curlで応答を確認し、リクエスト、ルート、Python関数、JSON応答の順にコードを追う | 処理の経路を短く説明できる |
| 9/10（木） | 1.5時間 | ステータスコードとレスポンス内容を確認するpytestを追加する | health APIの自動テストが通る |
| 9/11（金） | 1時間 | 起動・確認・テストの実行手順と疑問点を整理する | 同じコマンドで確認を繰り返せる |
| 9/12（土） | 3時間 | バックエンド停止時の接続失敗と、再起動後の復旧を確認する。不具合があれば修正する | 停止時は接続できず、再起動後は再び200になる |
| 9/13（日） | 3時間 | `application` フィールドを追加してテストを更新し、M0-C1の受け入れ条件と進捗を確認する | 小さな変更後もテストが通り、結果と未理解事項が記録される |

週の完了条件は、正常応答、自動テスト、停止時の失敗、再起動後の再現、処理経路の説明、小さな変更と記録がそろうこと。
PostgreSQLへの接続はW02、Reactからの表示とM0全体の確認はW03に予定している。

## 3. マイルストーン進捗

- [ ] M0: 開発基盤とエンドツーエンド疎通
- [ ] M1: トラブル記録の読み取り
- [ ] M2: 登録・更新と安全性
- [ ] M3: 検索と保守可能な構造
- [ ] M4: LLMによる単機能
- [ ] M5: 最小RAG
- [ ] M6: ベクトル検索と評価
- [ ] M7: 読み取り専用エージェント
- [ ] M8: 人間承認付きDB更新
- [ ] M9: 実務品質

## 4. 現在のマイルストーン進捗

### M0-C1: バックエンドの最小health API

- [x] Pythonと環境管理ツールを確認する（Python 3.12.3、uv 0.11.33）
- [x] `backend/` の最小構成を作成する
- [x] FastAPIアプリケーションを起動する
- [x] `GET /health` を実装する
- [x] `curl` またはブラウザでHTTP 200を確認する（ユーザーの問題解消報告に基づく）
- [ ] health APIのテストを追加する
- [ ] リクエストからレスポンスまでの流れを確認する

### M0-C2: PostgreSQLとバックエンドの疎通

- [ ] Docker ComposeでPostgreSQLを起動する
- [ ] 環境変数からDB接続情報を読む
- [ ] バックエンドから `SELECT 1` を実行する
- [ ] health APIへDB状態を追加する
- [ ] DB停止時の応答を確認する

### M0-C3: Reactとバックエンドの疎通

- [ ] `frontend/` を作成する
- [ ] React画面を起動する
- [ ] health APIを呼び出す
- [ ] Frontend、Backend、Databaseの状態を表示する
- [ ] loading、success、errorを確認する

### M0-C4: エンドツーエンド確認と再現手順

- [ ] 新しい状態から起動手順を再現する
- [ ] バックエンド停止時の画面表示を確認する
- [ ] DB停止時の画面表示を確認する
- [ ] READMEへ起動・確認手順を反映する
- [ ] M0の通過条件を確認する
- [ ] M0の振り返りを記録する

## 5. 完了したこと

- 2026-08-13: 初期ドキュメント構成を作成
  - `README.md`
  - `AGENTS.md`
  - `docs/ROADMAP.md`
  - `docs/PROGRESS.md`
  - `docs/milestones/README.md`
  - `docs/milestones/TEMPLATE.md`
  - `docs/milestones/M0.md`
- 2026-08-14: 合成データ方針をドキュメントへ反映
  - テーマは設備トラブル対応のまま継続
  - 実在する業務データは使用しない
  - 各マイルストーンで必要な合成データだけを段階的に追加
- 2026-09-03: 学習時間を基に全体期間と週次目標を設定
  - M8第一完成を2027-06-13、M9機能完了を2027-08-08に設定
  - 予備期間と最終確認を含む50週間の計画を `docs/SCHEDULE.md` に作成
  - 未来の詳細サイクルは各マイルストーン開始時に作る方針を維持
- 2026-09-04: プロジェクト概要と直近の週目標を確認
  - 文書とファイル構成から、M0-C1着手前で実装コード・テストがないことを確認
  - W01の目標を最小health APIの完成とし、標準12時間の日別アクションを整理
  - 今回は計画確認のみ。実装・テスト実行・環境の動作確認は未実施
- 2026-09-04: 9/7予定分を前倒しで開始し、環境を確認
  - Python 3.12.3、uv 0.11.33、Node.js v24.15.0、Docker Compose CLI v5.3.1を確認
  - システムPythonにpipとensurepipがないことを確認し、既存のuvで環境と依存関係を管理する手順を提示
  - 仮想環境・依存関係・Git除外設定の作成はユーザーが手順を実行後に確認する。API実装・テスト・Dockerの起動確認は未実施
- 2026-09-04: ユーザーが作成したバックエンド環境を確認し、9/7予定分を完了
  - `pyproject.toml`、`uv.lock`、`.venv` の存在と、専用PythonからのFastAPI・Uvicornのimport成功を確認
  - `.venv/bin/python` が `/usr/bin/python3` を参照し、Python 3.12.3を使っていることを確認
  - ルート `.gitignore` の `.env/` を予定どおり `.venv/` に修正。修正前はuvが仮想環境内に作った `.gitignore` によって除外されていた
  - ユーザーは仮想環境のPythonを使う点を理解。`.venv` には設定に加えPython実行用のリンクやライブラリもあること、`--python` は使用するPythonの指定であること、`git check-ignore` は除外状態を調べるコマンドであることを補足
  - API実装とAPIテストは未着手

- 2026-09-04: health APIが404になる原因を特定し、URL誤記を修正
  - ユーザーのログでUvicornの起動成功と `/health` の404を確認
  - `backend/main.py` のデコレーターが `/heath` だったため `/health` に修正
  - 修正前後のルート一覧を比較し、`/health` の登録を確認。URLを決めるのは関数名ではなくデコレーターであることを説明
  - Codex側のHTTP接続は失敗したため、修正後の200応答は未確認。ユーザー環境でcurlによる200と存在しないURLの404を再確認する

- 2026-09-04: ユーザーから問題解消の報告を受け、9/8予定分を完了
  - `/health` の修正後の正常応答確認をユーザー報告に基づき記録。Codex側でHTTP通信の再検証は行っていない
  - 次の作業を9/9予定の処理経路確認に更新。本人による処理経路の説明、自動テスト、停止・再起動確認、小さな仕様変更は引き続き未完了

## 6. 現在のブロッカー

なし。

ブロッカーが発生した場合は、次の形式で記録します。

```text
- 発生日:
- 現象:
- 再現手順:
- 期待結果:
- 実際の結果:
- 試したこと:
- 次に確認すること:
```

## 7. 未理解・後で深掘りする事項

現時点ではなし。

実装を止めずに後で確認する疑問を、次の形式で追加します。

```text
- 疑問:
- どの処理で出てきたか:
- 今すぐ理解が必要か: Yes / No
- 確認する予定のマイルストーン:
```

## 8. 保留している改善案

以下の学習記録案は2026-09-04時点の検討内容です。現在は [学習記録の運用](../learning/README.md) を参照してください。質問・回答は要約で残し、学習内容に関係のない相談は学習記録の対象外とします。

- 改善案: 質問・回答と理解の変化を残す学習記録を設け、現在地・作業履歴・学習記録の役割を分ける（2026-09-04相談、構成は未採用）
- ユーザーの目的: 当時何が分からず、何を質問し、どの回答や具体例から理解したのかを振り返る。
- 提案: `docs/PROGRESS.md` は現在地、次の1ステップ、ブロッカー、関連記録へのリンクを中心にする。詳細チェックは `docs/milestones/Mx.md`、質問・回答は `docs/learning/M0.md`、過去の作業・確認結果は `docs/history/M0.md` へ分ける。新規ファイルはまだ作成していない。
- 記録の要点: 日付・サイクル・質問ID、質問原文、当時の疑問、回答要点と具体例、本人の説明や確認結果、残る疑問、参照元を残す。原文と要約を区別し、回答しただけで理解済みとは判定しない。後日の理解や訂正は日付付きで追記する。
- 振り返り案: セッション終了時に質問・回答を記録し、週末に1〜3件を選んで回答を見ずに説明する。結果を同じ記録へ追記し、理解確認ができなくても開発を止めない。
- 期待効果: 作業再開時の見通しを保ち、実装の進捗と本人が確認できた理解をそれぞれ追える。
- 今は実装しない理由: 今回は仕組みと管理方針への意見を求める相談。構成変更と既存記録の移動は未実施。
- 再検討する時期: 学習記録の導入に着手するとき。最初に実際の質問・回答1件で書式と振り返り方法を試す。

現在のマイルストーンに不要な案は、実装せずここへ記録します。

```text
- 改善案:
- 期待効果:
- 今は実装しない理由:
- 再検討する時期:
```

## 9. 重要な決定事項

| 日付 | 決定 | 理由 | 影響 |
|---|---|---|---|
| 2026-08-14 | 設備トラブル対応というテーマを継続する | Webアプリ、RAG、エージェント、DB更新を一貫して学べるため | ロードマップのテーマとマイルストーンは変更しない |
| 2026-08-14 | 実際の業務データを使用しない | 業務外の個人学習であり、会社情報や業務情報を持ち出さないため | 設備、履歴、文書、利用者はすべて架空の合成データとする |
| 2026-08-14 | 合成データを必要な時点で段階的に作る | 過剰な事前設計を避け、短い開発サイクルを維持するため | M1以降の開始時に、必要件数と内容だけを具体化する |
| 2026-09-04 | バックエンドのPython環境と依存関係を既存のuvで管理する | uvが利用でき、システムPythonにはpipとensurepipがないため | `backend/pyproject.toml` と `backend/uv.lock` で依存関係を記録し、専用の `.venv/` を使う |

## 10. セッションログ

| 日付 | 実施内容 | 確認結果 | 次の1ステップ |
|---|---|---|---|
| 2026-08-13 | 初期ドキュメントを作成 | M0を開始できる構成を準備 | FastAPIの最小health APIを作る |
| 2026-08-14 | 合成データの利用方針を決定し、関連ドキュメントを更新 | テーマを維持し、実データを使わず段階的に合成データを追加する方針を明文化 | FastAPIの最小health APIを作る |
| 2026-09-03 | 週9〜16時間を前提にプロジェクト期間、マイルストーン期間、週次目標を設定 | M0〜M9と予備期間を50週間へ割り当てた | FastAPIの最小health APIを作る |
| 2026-09-04 | 概要、現在地、W01の目標と日別アクションを整理 | 文書とファイル構成を照合。M0-C1は未着手。HTTPリクエストからJSON応答までを最初の学習対象とし、実装・テスト・環境確認は次回実施 | FastAPIの最小health APIを作る |
| 2026-09-04 | 9/7予定分の環境確認を実施し、構成作成のアクションを提示 | Pythonとuvが利用可能。pipとensurepipの不足を確認。仮想環境と依存関係の役割を説明する手順を整理。構成作成とimport確認は未実施 | uvでバックエンドの最小構成と依存関係を用意する |
| 2026-09-04 | ユーザーの環境構築を検証し、仮想環境・Python指定・Git除外確認を説明 | 専用Pythonでimport成功。Git除外設定の誤記を修正。9/7予定分は完了、APIは未実装 | FastAPIの `GET /health` を作り、起動と正常応答を確認する |
| 2026-09-04 | 9/8予定分の実装・起動・確認手順を提示 | FastAPIとUvicornが利用可能であることを再確認。`backend/main.py` の最小コード、Uvicorn起動、curlによる200と存在しないURLの404確認、停止手順を提示。API作成とHTTP確認はユーザーの実行待ち | `backend/main.py` を作成して起動し、`GET /health` の200応答を確認する |
| 2026-09-04 | 質問・回答の学習記録とドキュメント分担を検討 | 文書と構成を確認し、進捗・履歴・学習記録の分離案を整理。主要概念は実装の進捗と理解の変化を別に記録すること。記録方式は未採用で、本人の理解確認とファイル分割は未実施。文書の差分確認のみで、実装・テスト実行はなし | 開発の次の作業は引き続き `GET /health` の作成と200応答の確認。学習記録案は第8節に保留 |
| 2026-09-04 | `/health` の404を調査し、`/heath` というURL誤記を修正 | サーバー起動はユーザーログで確認。修正前後のルート一覧を比較し、正しいURLの登録を確認。HTTPの200応答は未確認 | `curl -i http://127.0.0.1:8000/health` で再確認する |
| 2026-09-04 | ユーザーの問題解消報告を受け、9/8予定分の正常応答確認を完了として記録 | ユーザー環境での解消報告に基づく。CodexによるHTTP再検証は未実施。URLと関数の対応を説明済みだが、本人の処理経路の説明は次回確認 | 9/9予定分として、リクエストからJSON応答までをコードと対応付けて確認する |
